apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.api.dataVolumeClaim }}
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
    - ReadWriteMany
    - ReadOnlyMany
  resources:
    requests:
      storage: {{ .Values.api.dataVolumeSize }}